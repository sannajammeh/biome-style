// Template literals (even multi-line) and ordinary strings are allowed.
const templated = `line one
line two`;
const concatenated = 'a' + 'b';
const withBackslash = 'a backslash \\ in the middle';
const newlineEscape = 'a newline escape \n still on one line';
const plain = 'just a normal string';
const escapedQuote = 'it\'s fine';
