// Binary addition, increment, and compound assignment are all fine.
const sum = a + b;
const concat = 'x' + y;
let counter = 0;
counter++;
++counter;
counter += 1;
const coerced = Number(value);
const negated = -x;
