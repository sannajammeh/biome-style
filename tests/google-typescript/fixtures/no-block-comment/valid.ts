// Line comments are the preferred implementation-comment style.
// They may span several lines using consecutive `//` markers.
const lineCommented = 1;

/**
 * JSDoc block comments are explicitly allowed by the guide for
 * documenting declarations.
 */
const documented = 2;
