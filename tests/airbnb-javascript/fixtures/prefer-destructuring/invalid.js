// Object access whose binding name matches the property name — destructure it.
const x = obj.x;
const sameName = obj.sameName;
// First array element by literal index 0 — destructure the head instead.
const first = arr[0];
const head = items[0];
